export default {
    OAUTH_SERVER_REDIRECT_ENDPOINT: 'http://localhost:3001/callback',
    // Scopes we request from Google. Doesn't really matter as we only need login.
    GOOGLE_SCOPES: 'https://www.googleapis.com/auth/userinfo.email',
};